# 📜 СТАФФ-СПЕЦИФИКАЦИЯ И ШАБЛОН ГЕНЕРАТОРА (SOTA 2026 PROGRAMMATIC TEMPLATE)

## 📌 Архитектурный стандарт зафиксированного шаблона:

Каждая сгенерированная ИИ-страница подчиняется каноническому зафиксированному шаблону ЦЕХ Studio:

1. **Матричная заставка (SK-17 / M-12)**:
   - Хранит фразу в атрибуте `<canvas data-text="СТРОКА1|СТРОКА2|СТРОКА3|СТРОКА4">`.
   - Исполняется ровно в 3.5 сек с мгновенным стартом с 0-го кадра.
   - Включает пиксельный DPR-расчёт шрифтов на мобильных.

2. **Шапка и Навигация**:
   - Шапка `hud-header` со статусом ЦБ РФ и кнопкой повтора заставки.
   - Хлебные крошки (`Главная` -> `Имя категории` -> `Текущая ключевая фраза`).

3. **Телеметрический блок**:
   - 4 ячейки показателей: Ставка, Скорость, Макс. лимит, % Одобрения.

4. **Интерактивный Калькулятор**:
   - Слайдеры выбора суммы и срока с мгновенным расчётом на JS.

5. **Полная сетка офферов (`.catalog-grid`)**:
   - 100% Mobile Overflow Safe: одноколоночный режим на экранах `<= 768px`.
   - Строгая фиксация логотипов: `.offer-bank-logo` 48x48px (`object-fit: contain`).
   - Кнопки взаимодействия с трекингом `data-analytics-event`.

6. **SEO-секционная статья**:
   - Структурированные H2/H3 заголовки, параграфы и правила оформления без слоп-штампов.

7. **Schema.org Микроразметка**:
   - Валидный `FinancialService` / `FinancialProduct` JSON-LD блок.
